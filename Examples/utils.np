

///// Functions /////

int notavar = 911;

bool boing(int which = 0)
{
    bool boinged = false;

    if(which == 0)
    {
        which = (int)random(30, MAX.Value);
        boinged = true;
        print("boing");
    }

    return boinged;
}
